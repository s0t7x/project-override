# client/.env
# Environment variables for ProjectOverride Client (Vite)
# Variables MUST start with VITE_ to be exposed to the client code

# Endpoint for the Colyseus server
VITE_COLYSEUS_ENDPOINT="ws://localhost:2567"

# Optional: Client log level
# VITE_LOG_LEVEL="debug"